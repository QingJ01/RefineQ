# Third-party notices

RefineQ depends on open-source Python and Node.js packages declared in `pyproject.toml` and
`apps/web/package-lock.json`. Each dependency remains subject to its own license and copyright
notice. Production container builds install those packages from their published distributions.

No third-party source tree is vendored in this repository.

