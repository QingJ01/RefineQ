# 黑客松对抗性审查：代码整改记录

> 审查日期：2026-08-08
>
> 实施基线：origin/main@64515b1
>
> 范围：仓库内代码、配置、自动化测试和技术文档；访谈、部署执行和参赛材料不在本轮范围。
>
> 状态口径：只有“已实现”“基线已具备”“驳回（附不变量理由）”和“延期（非代码）”四种状态；没有把未验证事项写成已完成。

## 1. 第一性原理

本轮不以“把清单逐项改掉”为目标，而以这些不可妥协的不变量为判断标准：

1. 掌握度只能由独立、可归因且足够充分的学习证据改变；同一道题的重做不是新证据。
2. 模型输出是不可信建议，不是掌握度事实；确定性门槛必须在模型之外成立。
3. 数据库事务只包围一致性读写，不能跨越模型、Embedding、OCR 等外部网络调用。
4. 客户端身份只能由明确的可信代理提供；默认配置必须安全且可验证。
5. 计划、反馈和资料如果被保存，就必须被下游消费；否则不应宣称形成闭环。
6. 系统不能制造误区、主题或引用；需要推断时必须展示为建议并由用户确认。
7. 生产功能写入的数据必须位于持久卷，且备份源与备份目标必须物理分离。
8. 可观测性不能泄露答案、材料、令牌或稳定用户标识。

## 2. 可复现基线

在隔离工作树、提交 64515b1 上实测：

| 关卡 | 结果 |
| --- | --- |
| 后端 pytest | 352 passed, 3 skipped |
| 前端 Vitest | 144 passed |
| 前端 ESLint | 通过 |
| Next.js production build | 通过 |
| Playwright | 上一轮远端验证为 6 passed；合并前重新执行 |

## 3. 基线发现的最终状态

| 原编号 | 状态 | 结论 |
| --- | --- | --- |
| H-01 | 已实现 | 支持 M/D，并以相对日期约束优先于同句中的绝对日期；不把含糊的“下周”伪造成精确日期。见 `81126f0`。 |
| A-02 | 已实现 | README 首段改为“有讲义、截止日期，但无法判断主题掌握度”的具体处境。见 `81126f0`。 |
| C-04 | 基线已具备 | 生产数据库种子、`REFINEQ_DEMO_PASSWORD` 和 `refineq-demo` 已落地；继续禁止恢复命令接收明文密码参数。 |
| F-03 | 基线已具备 | 中日韩文本二元组词法检索已有生产接线和回归测试。 |
| F-06 | 已实现 | 删除未接线的自适应诊断、错误分析和推荐实现；`ModelSettingsRepository` 收敛为生产消费者真正依赖的结构接口。见 `d17b453`。 |

## 4. 被驳回的原方案

### B-02 不修改为“两个 streak 互不清零”

难度策略的业务规则是“连续两次独立同向证据才升降”。现实现会在相反结果出现时清空另一方向状态，符合该规则。原方案若保留两个计数器，会让“对、错、对”在第三次错误升级，直接违反连续语义。

处理：不改生产算法，只补参数化回归测试，覆盖连对、连错、交替、重做同题和边界等级。

### B-05 不改标准 BKT 转移方程

答错后的非零下界来自“每次学习机会之后仍可能发生学习”的标准 BKT 转移，不是数值错误。直接只在答对时应用 p_learn 会改变模型含义及全部历史数据。

处理：保留方程；把 evidence_count 与 mastery_is_stable 接到 API 和界面，只有概率、证据数量和定性门槛同时满足时才显示“已掌握”。

### D-01 不在降级判分中制造 misconception

回答缺失、偏题或证据不足只能形成 gap，不能凭空断言学习者存在某个误区。处理：渲染真实 misconceptions；空数组时不显示该栏。

### E-03 不自动把上传内容写成主题

材料是不可信数据，自动改写主题会破坏掌握度连续性。处理：只从标题和显式标签生成有界候选，展示为建议，用户确认后再原子地追加主题。

## 5. 有效缺陷的落地结果

| 领域 | 状态 | 落地结果 | 提交 |
| --- | --- | --- | --- |
| 独立学习证据 | 已实现 | 同一 `question_id` 每个主题只计入一次；重做仍可获得反馈，但不再改变 BKT 或难度。稳定掌握同时要求概率、证据数和定性门槛。 | `7fd179d` |
| AI 判分边界 | 已实现 | 掌握证据必须同时通过确定性的本地实质性门槛和模型门槛；模型不能单方面修改掌握度。 | `7fd179d` |
| 认证限流与代理身份 | 已实现 | 密码重置复用认证限流桶；API 只接受确定的 Caddy 代理地址提供的转发身份，默认配置不再信任任意来源。 | `19474cf` |
| 最弱主题排序 | 已实现 | 前后端以同一稳定次级键处理掌握度平局。 | `81126f0` |
| 健康检查与备份 | 已实现 | `/health` 由 Caddy 直接路由；健康探测缩短启动等待；管理备份写入独立 `/backups` 持久卷而非只读根或源数据卷。 | `e0c0168` |
| 反馈真实性与日志最小化 | 已实现 | 仅展示真实 misconceptions 和实际 AI/降级模式；教练使用可读主题名；降级日志只保留事件、原因、耗时和模式。 | `0e169a1` |
| 计划闭环 | 已实现 | `plan_session_id` 决定主题与练习模式；只有形成有效掌握证据的作答才完成来源计划会话。 | `ffc88e9` |
| 学习反馈闭环 | 已实现 | 下一题只消费最近三次同主题 gaps/misconceptions，并将其明确标为有界、不可信上下文。 | `ffc88e9` |
| 初始诊断入口 | 已实现 | 前端暴露每空间一次的有界初始自评，并明确称为自评，不冒充自适应 AI 诊断。 | `b6f14f4` |
| 材料主题建议 | 已实现 | 只从有界标题和显式标签生成候选；不读取正文推断主题；必须由用户确认后才同步写入空间和学习状态，失败时补偿回滚。 | `dd6fca2` |
| 短事务与幂等 | 已实现 | 问题生成、空间路由和教练回复采用“快照→事务外模型调用→条件提交”；提交时重查幂等键、配额和相关状态。嵌套 PostgreSQL owner scope 各自获取 advisory lock。 | `4b0ec22` |
| 检索连续性与排名 | 已实现 | 分块优先句段边界并保留约 12% 重叠；词法和语义结果使用加权 RRF，低于阈值的语义噪声不参与融合。 | `8e8998a` |
| 虚假能力表面积 | 已实现 | 删除只被孤立测试调用的学习引擎和文件模型配置实现，保留已接线的初始诊断、证据模型和平台配置。 | `d17b453` |

## 6. 解决方案与可证伪验收

| 领域 | 最小正确方案 | 失败时必须暴露的测试 |
| --- | --- | --- |
| 独立证据 | 每个主题保存最近已计入的 question_id；首次有效证据后，同题任何重做均不再改变 BKT 或难度 | 同题错→错、错→对、对→错只更新一次；不同题正常累计 |
| AI 判分 | 本地实质性门槛 AND 模型 sufficient_evidence；二者任一为假都不更新掌握度 | 空白式、复读、偏题和模型误报不更新；充分答案更新 |
| 稳定掌握 | API 返回每主题 stable；界面只对 stable=true 显示“已掌握” | 高概率但证据少仍显示“进行中” |
| 代理安全 | Compose 使用确定的代理地址，API 只信任该地址；禁止默认星号 | 直连伪造头不能换 key；可信代理能区分终端客户端 |
| 备份 | REFINEQ_BACKUP_ROOT 指向独立 /backups 持久卷 | 只读根文件系统拓扑下创建、重建后列出、校验均成功 |
| 计划闭环 | plan_session_id 决定 topic 与 mode；有效作答完成该 session | learn/practice/apply/review 分别映射 concept/case/project/exam |
| 反馈闭环 | 最近同主题的 gaps/misconceptions 作为有界不可信上下文交给下一题 | 其他主题不泄漏；最多取最近三次；提示词明确不可信 |
| 事务 | 外部调用前后分别使用短事务，提交时重新检查幂等键和版本 | 阻塞 transport 时可从第二连接读取；并发相同 request/turn 只落一份 |
| 检索 | 句段优先分块并保留约 12% overlap；使用带阈值的加权 RRF | 跨边界事实可检索；低相似度向量不返回；排名对尺度不敏感 |
| 可见性 | 只渲染真实字段；日志只含事件、原因、耗时和模式 | 日志中不出现答案、材料正文、token、邮箱或 owner_id |

每项都先用失败测试证伪旧行为，再做最小实现并运行相关回归。逐步命令、文件和 RED/GREEN 顺序见同目录的 `2026-08-08-hackathon-audit-remediation-implementation.md`。

## 7. 验证边界

- 合并前本地验证结果：Python `365 passed, 3 skipped`；Vitest `150 passed`；Playwright `6 passed`；Ruff check、Ruff format、密钥扫描、ESLint 和 Next.js production build 全部通过。
- 本机没有 Docker CLI，因此不把本地 `docker compose config` 写成已验证；Compose 的静态不变量由部署契约测试覆盖，渲染和运行时验证留给具备 Docker 的 CI/部署环境。
- “代码可部署”不等于“已经部署”。本轮不产生线上可用性、真实访谈或参赛成效证据。

## 8. 延期的非代码清单

部署执行、真实访谈、参赛提交材料、路演录屏和线上监控接入按范围明确延期，由后续单独处理；本轮只保证仓库能够安全支持这些活动，不把它们包装成已完成。
